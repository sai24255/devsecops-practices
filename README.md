# devsecops-practices
1. .gitnore looks for files not to track
2. to track patterns inside your scripts. use pre-commit hooks or pre-commit frame work use in    pipeline will solve unwanted merge
3. use local pre-commit file to manage locally inside .git/hooks/pre-commit
